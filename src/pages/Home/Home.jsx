import React from 'react';
import PropTypes from 'prop-types';
import HomeBanner from './homeBanner/HomeBanner';

const Home = props => {
    return (
        <div>
            <HomeBanner/>
        </div>
    );
};

Home.propTypes = {
    
};

export default Home;